# Architecture Decisions Directory

All Architecture Decision Records (ADRs) are stored in this folder.

Files must follow this naming convention:
`decisions/XXXX-short-descriptive-name.md`

Where `XXXX` represents the 4-digit zero-padded index increment (e.g. `0001-use-postgresql.md`).
